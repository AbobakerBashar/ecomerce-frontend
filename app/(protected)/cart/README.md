Cart route placeholder.

- Protected group: `app/(protected)`
- Route: `app/(protected)/cart/page.tsx`
- Current implementation uses localStorage (`demo_cart_items`) because no auth/cart backend exists in the repo yet.
